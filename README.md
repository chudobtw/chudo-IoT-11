  # chudo-IoT-11
  Write a program in the algorithmic language Python according to the task received from the teacher in Table 1. Add the appropriate constructors, destructors, access functions and display information on the screen. In the main method, demonstrate the operation of the classes.

  Mine variant - 19th.
  Create a class Pet (name, breed, age, greeting, mass). Add enum kind: DOG, CAT, BIRD, …. . Add a class Home, which contains a specific list of pets. Add a function areFriends( Pet*) that takes a list of animals as input and finds friends among them (two animals are considered friends if the age difference between them is less than 2 years). Add a method isPolite(self) to the Pet class: if greeting contains “Hello”, return True, otherwise return False. Add a function to sort by age.

